/**
 * Main entry point for the DOM tagger
 */

export { DOMTagger } from './tagger';
export type { 
  TaggerConfig, 
  TaggedElement
} from './types';

// Default export
export { default } from './tagger'; 